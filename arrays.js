const prompt = require('prompt-sync')();

let nomes = ["Cauã", "Breno", "Gabriel", "Augusto"]

    for(let indice = 0; indice < nomes.length; indice++) {
        console.log(`Pessoa ${indice} Nome: ${nomes[indice]}`)
    }

//console.log("// USANDO WHILE")

//let repetição = 0

  //  while(repetição < nomes.length){
    //    console.log(`Pessoa ${repetição} Nome: ${nomes[repetição]}`)   
    //repetição++
    // }
